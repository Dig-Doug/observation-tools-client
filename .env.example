# Observation Tools Server Configuration
# Copy this file to .env and adjust as needed

# Server host (default: 0.0.0.0)
HOST=0.0.0.0

# Server port (default: 3000)
PORT=3000

# Directory for storing data (default: .observation-tools)
# In Docker, this is typically /data with a volume mount
DATA_DIR=.observation-tools

# Directory for static files (default: crates/observation-tools-server/static)
# In Docker, this is /app/static
# STATIC_DIR=/app/static

# Logging level (default: info)
# Options: trace, debug, info, warn, error
RUST_LOG=info
